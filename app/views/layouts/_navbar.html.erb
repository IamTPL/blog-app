<nav class="navbar navbar-expand-lg bg-dark bg-body-tertiary text-white px-5" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand text-white fs-2" href="/">Blog</a>
       
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link fs-5 text-white" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fs-5" href="/articles">Articles List</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fs-5" href="/authors">Authors List</a>
                </li>
            </ul>
        </div>
    </div>
    
    <div class="d-flex align-items-center ms-auto flex-nowrap">
    <% if logged_in? %>
        <%= link_to "#{current_user.name}", current_user, class: "text-white fs-5 fw-bold px-2" %>
        <b>|</b>
        <%= link_to 'LogOut', logout_path, method: :delete, class: "text-white fs-5 fw-bold px-2" %>
    <% else %>
        <%= link_to 'LogIn', login_path, class: "text-white fs-5 fw-bold px-2" %>
    <% end %>
    </div>
</nav>

<div class="container">
    <% flash.each do |name, msg| %>
    <div class="alert alert-<%= name == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">
        <%= msg %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <% end %> 
</div>